#Para importar en pyhton:
from archivo import *

#Para introducir un dato:
jugador = int(input("Introduzca el dato"))

#Condicionales if:
if dato != 1:

#Bucle for

for in range(n):

for jugada in jugadas:

#Para definir diferentes posibilidades:

match dato:
	case 0:
    	print("Empate")
    case 1:
    	print("Gana jugador 1")
        
#Definir una funcion:

def Funcion():
    
#Clases:

from dataclasses import dataclass
import numpy as np

class Nodo:
    tablero: np.array
    vacias: int
    N: int

    def __init__(self, tablero):
        self.tablero = tablero
        self.N = self.tablero.shape[0]
        self.vacias = len(np.where(tablero == 0)[0])






    

        